<%= form_for(@diff) do |f| %>
  <% if @diff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@diff.errors.count, "error") %> prohibited this diff from being saved:</h2>

      <ul>
      <% @diff.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
