<h1>Listing diffs</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @diffs.each do |diff| %>
  <tr>
    <td><%= link_to 'Show', diff %></td>
    <td><%= link_to 'Edit', edit_diff_path(diff) %></td>
    <td><%= link_to 'Destroy', diff, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Diff', new_diff_path %>
