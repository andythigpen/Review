Review Status Change

The status of your review for 
"<%= @changeset.review_event.name %>" has changed to <% if @changeset.accepted? %>Accepted<% elsif @changeset.rejected? %>Rejected<% else %>Unknown<% end %> for changeset "<%= @changeset.name %>".  

URL: <%= @url %>

Signed,
The Review Swami

