Review Status Change

<%= @reviewee.username %> has <% if @status.accepted %>accepted<% else %>rejected<% end %> the "<%= @changeset.name %>" changeset for the "<%= @changeset.review_event.name %>" review event.

URL: <%= @url %>

Signed,
The Review Swami

