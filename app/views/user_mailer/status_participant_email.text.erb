Review Status Change: <% if @status.accepted? %>ACCEPTED<% elsif @status.rejected? %>REJECTED<% else %>ABSTAINED<% end %>

<% if @status.accepted? or @status.rejected? %>The user <%= @reviewer.full_name %> (<%= @reviewer.email %>) has <% if @status.accepted? %>accepted<% else %>rejected<% end %> the "<%= @changeset.name %>" changeset for the review "<%= @changeset.review_event.name %>".
<% else %>The user <%= @reviewer.full_name %> (<%= @reviewer.email %>) has requested to be removed from the review "<%= @changeset.review_event.name %>".<% end %>

Review URL: <%= @url %><%= review_event_path(@changeset.review_event) %>

Comment:
<%= @status.comments.first.content %>

Signed,
The Review Swami

